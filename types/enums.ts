export enum MetricsTabEnum {
  Business = "Business",
  Online = "Online",
  Training = "Training",
  System = "System",
}

export enum RecommendationTabEnum {
  Cohort = "Cohort",
  User = "User",
}

export enum RecommendationTabEnumNew {
  Rank = "Rank",
  Similar = "Similar",
  Search = "Search",
}

export enum MetricType {
  SUMMARY_METRIC = "Summary Metric",
  EVALUATION_METRIC = "Evaluation Metric",
}

export enum LiveMetricsDuration {
  LAST_WEEK = "Last week",
  LAST_MONTH = "Last month",
}

export enum TrainMetricsDuration {
  LAST_RUN = "Last run",
  LAST_WEEK = "Last week",
  LAST_MONTH = "Last month",
}

export enum DataType {
  DateTime = "datetime",
  Int = "int",
  Float = "float",
  String = "string",
  Unknown = "unknown",
  Operator = "operator",
  Bool = "bool",
}
